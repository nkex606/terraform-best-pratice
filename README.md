# terraform-best-pratice

terraform example for creating resources on GCP

terraform cloud configuration is config in `terraform.tf`

this example creates below:
  - a vpc
  - a subnet
  - firewall rules(allow)
  - a gce instance
  - a attached disk for gce